<!DOCTYPE html>
<html lang="en" dir="ltr">
  <head>
    <meta charset="utf-8">
    <title>Recipes</title>
  </head>
  <body>

    <% if @recipes %>

      <% @recipes.each do |recipe| %>

      <h2><%= recipe.name %></h2>
      <p><%= recipe.ingredients %></p>
      <p><%= recipe.cook_time %></p>
      <a href="/recipes/<%= recipe.id %>">Show me this recipe<a>

      <hr>

      <% end %>

      <% else %>
      <p>No recipes found...</p>
    <% end %>

    <a href="/recipes/new">New recipe</a>

  </body>
</html>
